<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to "Home", root_path, class: "navbar-brand" %>

    <div>
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <span class="nav-link">Üdv, <%= current_user.username %></span>
          </li>
          <li class="nav-item">
            <%= link_to "Profil módosítása", edit_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to "Kijelentkezés", destroy_user_session_path, method: :delete, class: "btn btn-link nav-link" %>          
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Bejelentkezés", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Regisztráció", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>